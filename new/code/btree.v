(* autogenerated from btree *)
Require Export New.code.fmt.
Require Export New.code.io.
Require Export New.code.sort.
Require Export New.code.strings.
Require Export New.code.sync.

From New.golang Require Import defn.
Definition btree : go_string := "btree".

Module btree.

Module Item. Definition id : go_string := "btree.Item"%go. End Item.
Module FreeList. Definition id : go_string := "btree.FreeList"%go. End FreeList.
Module ItemIterator. Definition id : go_string := "btree.ItemIterator"%go. End ItemIterator.
Module items. Definition id : go_string := "btree.items"%go. End items.
Module children. Definition id : go_string := "btree.children"%go. End children.
Module node. Definition id : go_string := "btree.node"%go. End node.
Module copyOnWriteContext. Definition id : go_string := "btree.copyOnWriteContext"%go. End copyOnWriteContext.
Module toRemove. Definition id : go_string := "btree.toRemove"%go. End toRemove.
Module direction. Definition id : go_string := "btree.direction"%go. End direction.
Module BTree. Definition id : go_string := "btree.BTree"%go. End BTree.
Module freeType. Definition id : go_string := "btree.freeType"%go. End freeType.
Module Int. Definition id : go_string := "btree.Int"%go. End Int.

Section code.
Context `{ffi_syntax}.


Definition Item : go_type := interfaceT.
#[global] Typeclasses Opaque Item.
#[global] Opaque Item.

Definition DefaultFreeListSize : Z := 32.

Definition nilItems : go_string := "btree.nilItems"%go.

Definition nilChildren : go_string := "btree.nilChildren"%go.

Definition FreeList : go_type := structT [
  "mu" :: sync.Mutex;
  "freelist" :: sliceT
].
#[global] Typeclasses Opaque FreeList.
#[global] Opaque FreeList.

Definition NewFreeList : go_string := "btree.NewFreeList"%go.

(* NewFreeList creates a new free list.
   size is the maximum size of the returned free list.

   go: btree.go:92:6 *)
Definition NewFreeListⁱᵐᵖˡ : val :=
  λ: "size",
    exception_do (let: "size" := (mem.alloc "size") in
    return: (mem.alloc (let: "$freelist" := (slice.make3 #ptrT #(W64 0) (![#intT] "size")) in
     struct.make #FreeList [{
       "mu" ::= type.zero_val #sync.Mutex;
       "freelist" ::= "$freelist"
     }]))).

Definition items : go_type := sliceT.
#[global] Typeclasses Opaque items.
#[global] Opaque items.

Definition children : go_type := sliceT.
#[global] Typeclasses Opaque children.
#[global] Opaque children.

Definition node : go_type := structT [
  "items" :: items;
  "children" :: children;
  "cow" :: ptrT
].
#[global] Typeclasses Opaque node.
#[global] Opaque node.

(* go: btree.go:96:20 *)
Definition FreeList__newNodeⁱᵐᵖˡ : val :=
  λ: "f" <>,
    exception_do (let: "n" := (mem.alloc (type.zero_val #ptrT)) in
    let: "f" := (mem.alloc "f") in
    do:  ((method_call #(ptrT.id sync.Mutex.id) #"Lock"%go (struct.field_ref #FreeList #"mu"%go (![#ptrT] "f"))) #());;;
    let: "index" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := ((let: "$a0" := (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) in
    slice.len "$a0") - #(W64 1)) in
    do:  ("index" <-[#intT] "$r0");;;
    (if: int_lt (![#intT] "index") #(W64 0)
    then
      do:  ((method_call #(ptrT.id sync.Mutex.id) #"Unlock"%go (struct.field_ref #FreeList #"mu"%go (![#ptrT] "f"))) #());;;
      return: (mem.alloc (type.zero_val #node))
    else do:  #());;;
    let: "$r0" := (![#ptrT] (slice.elem_ref #ptrT (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) (![#intT] "index"))) in
    do:  ("n" <-[#ptrT] "$r0");;;
    let: "$r0" := #null in
    do:  ((slice.elem_ref #ptrT (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) (![#intT] "index")) <-[#ptrT] "$r0");;;
    let: "$r0" := (let: "$s" := (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) in
    slice.slice #ptrT "$s" #(W64 0) (![#intT] "index")) in
    do:  ((struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f")) <-[#sliceT] "$r0");;;
    do:  ((method_call #(ptrT.id sync.Mutex.id) #"Unlock"%go (struct.field_ref #FreeList #"mu"%go (![#ptrT] "f"))) #());;;
    return: (![#ptrT] "n")).

(* freeNode adds the given node to the list, returning true if it was added
   and false if it was discarded.

   go: btree.go:112:20 *)
Definition FreeList__freeNodeⁱᵐᵖˡ : val :=
  λ: "f" "n",
    exception_do (let: "out" := (mem.alloc (type.zero_val #boolT)) in
    let: "f" := (mem.alloc "f") in
    let: "n" := (mem.alloc "n") in
    do:  ((method_call #(ptrT.id sync.Mutex.id) #"Lock"%go (struct.field_ref #FreeList #"mu"%go (![#ptrT] "f"))) #());;;
    (if: int_lt (let: "$a0" := (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) in
    slice.len "$a0") (let: "$a0" := (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) in
    slice.cap "$a0")
    then
      let: "$r0" := (let: "$a0" := (![#sliceT] (struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f"))) in
      let: "$a1" := ((let: "$sl0" := (![#ptrT] "n") in
      slice.literal #ptrT ["$sl0"])) in
      (slice.append #ptrT) "$a0" "$a1") in
      do:  ((struct.field_ref #FreeList #"freelist"%go (![#ptrT] "f")) <-[#sliceT] "$r0");;;
      let: "$r0" := #true in
      do:  ("out" <-[#boolT] "$r0")
    else do:  #());;;
    do:  ((method_call #(ptrT.id sync.Mutex.id) #"Unlock"%go (struct.field_ref #FreeList #"mu"%go (![#ptrT] "f"))) #());;;
    return: (![#boolT] "out")).

Definition ItemIterator : go_type := funcT.
#[global] Typeclasses Opaque ItemIterator.
#[global] Opaque ItemIterator.

Definition New : go_string := "btree.New"%go.

Definition NewWithFreeList : go_string := "btree.NewWithFreeList"%go.

(* New creates a new B-Tree with the given degree.

   New(2), for example, will create a 2-3-4 tree (each node contains 1-3 items
   and 2-4 children).

   go: btree.go:131:6 *)
Definition Newⁱᵐᵖˡ : val :=
  λ: "degree",
    exception_do (let: "degree" := (mem.alloc "degree") in
    return: (let: "$a0" := (![#intT] "degree") in
     let: "$a1" := (let: "$a0" := #(W64 DefaultFreeListSize) in
     (func_call #NewFreeList) "$a0") in
     (func_call #NewWithFreeList) "$a0" "$a1")).

Definition BTree : go_type := structT [
  "degree" :: intT;
  "length" :: intT;
  "root" :: ptrT;
  "cow" :: ptrT
].
#[global] Typeclasses Opaque BTree.
#[global] Opaque BTree.

Definition copyOnWriteContext : go_type := structT [
  "freelist" :: ptrT
].
#[global] Typeclasses Opaque copyOnWriteContext.
#[global] Opaque copyOnWriteContext.

(* NewWithFreeList creates a new B-Tree that uses the given node free list.

   go: btree.go:136:6 *)
Definition NewWithFreeListⁱᵐᵖˡ : val :=
  λ: "degree" "f",
    exception_do (let: "f" := (mem.alloc "f") in
    let: "degree" := (mem.alloc "degree") in
    (if: int_leq (![#intT] "degree") #(W64 1)
    then
      do:  (let: "$a0" := (interface.make #stringT.id #"bad degree"%go) in
      Panic "$a0")
    else do:  #());;;
    return: (mem.alloc (let: "$degree" := (![#intT] "degree") in
     let: "$cow" := (mem.alloc (let: "$freelist" := (![#ptrT] "f") in
     struct.make #copyOnWriteContext [{
       "freelist" ::= "$freelist"
     }])) in
     struct.make #BTree [{
       "degree" ::= "$degree";
       "length" ::= type.zero_val #intT;
       "root" ::= type.zero_val #ptrT;
       "cow" ::= "$cow"
     }]))).

(* insertAt inserts a value into the given index, pushing all subsequent values
   forward.

   go: btree.go:151:17 *)
Definition items__insertAtⁱᵐᵖˡ : val :=
  λ: "s" "index" "item",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "item" := (mem.alloc "item") in
    let: "index" := (mem.alloc "index") in
    let: "$r0" := (let: "$a0" := (![#items] (![#ptrT] "s")) in
    let: "$a1" := ((let: "$sl0" := #interface.nil in
    slice.literal #Item ["$sl0"])) in
    (slice.append #Item) "$a0" "$a1") in
    do:  ((![#ptrT] "s") <-[#items] "$r0");;;
    (if: int_lt (![#intT] "index") (let: "$a0" := (![#items] (![#ptrT] "s")) in
    slice.len "$a0")
    then
      do:  (let: "$a0" := (let: "$s" := (![#items] (![#ptrT] "s")) in
      slice.slice #Item "$s" ((![#intT] "index") + #(W64 1)) (slice.len "$s")) in
      let: "$a1" := (let: "$s" := (![#items] (![#ptrT] "s")) in
      slice.slice #Item "$s" (![#intT] "index") (slice.len "$s")) in
      (slice.copy #Item) "$a0" "$a1")
    else do:  #());;;
    let: "$r0" := (![#Item] "item") in
    do:  ((slice.elem_ref #Item (![#items] (![#ptrT] "s")) (![#intT] "index")) <-[#Item] "$r0");;;
    return: #()).

(* removeAt removes a value at a given index, pulling all subsequent values
   back.

   go: btree.go:161:17 *)
Definition items__removeAtⁱᵐᵖˡ : val :=
  λ: "s" "index",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "index" := (mem.alloc "index") in
    let: "item" := (mem.alloc (type.zero_val #Item)) in
    let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (![#ptrT] "s")) (![#intT] "index"))) in
    do:  ("item" <-[#Item] "$r0");;;
    do:  (let: "$a0" := (let: "$s" := (![#items] (![#ptrT] "s")) in
    slice.slice #Item "$s" (![#intT] "index") (slice.len "$s")) in
    let: "$a1" := (let: "$s" := (![#items] (![#ptrT] "s")) in
    slice.slice #Item "$s" ((![#intT] "index") + #(W64 1)) (slice.len "$s")) in
    (slice.copy #Item) "$a0" "$a1");;;
    let: "$r0" := #interface.nil in
    do:  ((slice.elem_ref #Item (![#items] (![#ptrT] "s")) ((let: "$a0" := (![#items] (![#ptrT] "s")) in
    slice.len "$a0") - #(W64 1))) <-[#Item] "$r0");;;
    let: "$r0" := (let: "$s" := (![#items] (![#ptrT] "s")) in
    slice.slice #Item "$s" #(W64 0) ((let: "$a0" := (![#items] (![#ptrT] "s")) in
    slice.len "$a0") - #(W64 1))) in
    do:  ((![#ptrT] "s") <-[#items] "$r0");;;
    return: (![#Item] "item")).

(* pop removes and returns the last element in the list.

   go: btree.go:170:17 *)
Definition items__popⁱᵐᵖˡ : val :=
  λ: "s" <>,
    exception_do (let: "out" := (mem.alloc (type.zero_val #Item)) in
    let: "s" := (mem.alloc "s") in
    let: "index" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := ((let: "$a0" := (![#items] (![#ptrT] "s")) in
    slice.len "$a0") - #(W64 1)) in
    do:  ("index" <-[#intT] "$r0");;;
    let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (![#ptrT] "s")) (![#intT] "index"))) in
    do:  ("out" <-[#Item] "$r0");;;
    let: "$r0" := #interface.nil in
    do:  ((slice.elem_ref #Item (![#items] (![#ptrT] "s")) (![#intT] "index")) <-[#Item] "$r0");;;
    let: "$r0" := (let: "$s" := (![#items] (![#ptrT] "s")) in
    slice.slice #Item "$s" #(W64 0) (![#intT] "index")) in
    do:  ((![#ptrT] "s") <-[#items] "$r0");;;
    return: (![#Item] "out")).

(* truncate truncates this instance at index so that it contains only the
   first index items. index must be less than or equal to length.

   go: btree.go:180:17 *)
Definition items__truncateⁱᵐᵖˡ : val :=
  λ: "s" "index",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "index" := (mem.alloc "index") in
    let: "toClear" := (mem.alloc (type.zero_val #items)) in
    let: "$r0" := (let: "$s" := (![#items] (![#ptrT] "s")) in
    slice.slice #Item "$s" #(W64 0) (![#intT] "index")) in
    let: "$r1" := (let: "$s" := (![#items] (![#ptrT] "s")) in
    slice.slice #Item "$s" (![#intT] "index") (slice.len "$s")) in
    do:  ((![#ptrT] "s") <-[#items] "$r0");;;
    do:  ("toClear" <-[#items] "$r1");;;
    (for: (λ: <>, int_gt (let: "$a0" := (![#items] "toClear") in
    slice.len "$a0") #(W64 0)); (λ: <>, #()) := λ: <>,
      let: "$r0" := (let: "$s" := (![#items] "toClear") in
      slice.slice #Item "$s" (let: "$a0" := (![#items] "toClear") in
      let: "$a1" := (![#items] (globals.get #nilItems)) in
      (slice.copy #Item) "$a0" "$a1") (slice.len "$s")) in
      do:  ("toClear" <-[#items] "$r0"));;;
    return: #()).

(* find returns the index where the given item should be inserted into this
   list.  'found' is true if the item already exists in the list at the given
   index.

   go: btree.go:191:16 *)
Definition items__findⁱᵐᵖˡ : val :=
  λ: "s" "item",
    exception_do (let: "found" := (mem.alloc (type.zero_val #boolT)) in
    let: "index" := (mem.alloc (type.zero_val #intT)) in
    let: "s" := (mem.alloc "s") in
    let: "item" := (mem.alloc "item") in
    let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := (let: "$a0" := (let: "$a0" := (![#items] "s") in
    slice.len "$a0") in
    let: "$a1" := (λ: "i",
      exception_do (let: "i" := (mem.alloc "i") in
      return: (let: "$a0" := (![#Item] (slice.elem_ref #Item (![#items] "s") (![#intT] "i"))) in
       (interface.get #"Less"%go (![#Item] "item")) "$a0"))
      ) in
    (func_call #sort.Search) "$a0" "$a1") in
    do:  ("i" <-[#intT] "$r0");;;
    (if: (int_gt (![#intT] "i") #(W64 0)) && (~ (let: "$a0" := (![#Item] "item") in
    (interface.get #"Less"%go (![#Item] (slice.elem_ref #Item (![#items] "s") ((![#intT] "i") - #(W64 1))))) "$a0"))
    then return: ((![#intT] "i") - #(W64 1), #true)
    else do:  #());;;
    return: (![#intT] "i", #false)).

(* insertAt inserts a value into the given index, pushing all subsequent values
   forward.

   go: btree.go:206:20 *)
Definition children__insertAtⁱᵐᵖˡ : val :=
  λ: "s" "index" "n",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "n" := (mem.alloc "n") in
    let: "index" := (mem.alloc "index") in
    let: "$r0" := (let: "$a0" := (![#children] (![#ptrT] "s")) in
    let: "$a1" := ((let: "$sl0" := #null in
    slice.literal #ptrT ["$sl0"])) in
    (slice.append #ptrT) "$a0" "$a1") in
    do:  ((![#ptrT] "s") <-[#children] "$r0");;;
    (if: int_lt (![#intT] "index") (let: "$a0" := (![#children] (![#ptrT] "s")) in
    slice.len "$a0")
    then
      do:  (let: "$a0" := (let: "$s" := (![#children] (![#ptrT] "s")) in
      slice.slice #ptrT "$s" ((![#intT] "index") + #(W64 1)) (slice.len "$s")) in
      let: "$a1" := (let: "$s" := (![#children] (![#ptrT] "s")) in
      slice.slice #ptrT "$s" (![#intT] "index") (slice.len "$s")) in
      (slice.copy #ptrT) "$a0" "$a1")
    else do:  #());;;
    let: "$r0" := (![#ptrT] "n") in
    do:  ((slice.elem_ref #ptrT (![#children] (![#ptrT] "s")) (![#intT] "index")) <-[#ptrT] "$r0");;;
    return: #()).

(* removeAt removes a value at a given index, pulling all subsequent values
   back.

   go: btree.go:216:20 *)
Definition children__removeAtⁱᵐᵖˡ : val :=
  λ: "s" "index",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "index" := (mem.alloc "index") in
    let: "n" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (![#ptrT] (slice.elem_ref #ptrT (![#children] (![#ptrT] "s")) (![#intT] "index"))) in
    do:  ("n" <-[#ptrT] "$r0");;;
    do:  (let: "$a0" := (let: "$s" := (![#children] (![#ptrT] "s")) in
    slice.slice #ptrT "$s" (![#intT] "index") (slice.len "$s")) in
    let: "$a1" := (let: "$s" := (![#children] (![#ptrT] "s")) in
    slice.slice #ptrT "$s" ((![#intT] "index") + #(W64 1)) (slice.len "$s")) in
    (slice.copy #ptrT) "$a0" "$a1");;;
    let: "$r0" := #null in
    do:  ((slice.elem_ref #ptrT (![#children] (![#ptrT] "s")) ((let: "$a0" := (![#children] (![#ptrT] "s")) in
    slice.len "$a0") - #(W64 1))) <-[#ptrT] "$r0");;;
    let: "$r0" := (let: "$s" := (![#children] (![#ptrT] "s")) in
    slice.slice #ptrT "$s" #(W64 0) ((let: "$a0" := (![#children] (![#ptrT] "s")) in
    slice.len "$a0") - #(W64 1))) in
    do:  ((![#ptrT] "s") <-[#children] "$r0");;;
    return: (![#ptrT] "n")).

(* pop removes and returns the last element in the list.

   go: btree.go:225:20 *)
Definition children__popⁱᵐᵖˡ : val :=
  λ: "s" <>,
    exception_do (let: "out" := (mem.alloc (type.zero_val #ptrT)) in
    let: "s" := (mem.alloc "s") in
    let: "index" := (mem.alloc (type.zero_val #intT)) in
    let: "$r0" := ((let: "$a0" := (![#children] (![#ptrT] "s")) in
    slice.len "$a0") - #(W64 1)) in
    do:  ("index" <-[#intT] "$r0");;;
    let: "$r0" := (![#ptrT] (slice.elem_ref #ptrT (![#children] (![#ptrT] "s")) (![#intT] "index"))) in
    do:  ("out" <-[#ptrT] "$r0");;;
    let: "$r0" := #null in
    do:  ((slice.elem_ref #ptrT (![#children] (![#ptrT] "s")) (![#intT] "index")) <-[#ptrT] "$r0");;;
    let: "$r0" := (let: "$s" := (![#children] (![#ptrT] "s")) in
    slice.slice #ptrT "$s" #(W64 0) (![#intT] "index")) in
    do:  ((![#ptrT] "s") <-[#children] "$r0");;;
    return: (![#ptrT] "out")).

(* truncate truncates this instance at index so that it contains only the
   first index children. index must be less than or equal to length.

   go: btree.go:235:20 *)
Definition children__truncateⁱᵐᵖˡ : val :=
  λ: "s" "index",
    exception_do (let: "s" := (mem.alloc "s") in
    let: "index" := (mem.alloc "index") in
    let: "toClear" := (mem.alloc (type.zero_val #children)) in
    let: "$r0" := (let: "$s" := (![#children] (![#ptrT] "s")) in
    slice.slice #ptrT "$s" #(W64 0) (![#intT] "index")) in
    let: "$r1" := (let: "$s" := (![#children] (![#ptrT] "s")) in
    slice.slice #ptrT "$s" (![#intT] "index") (slice.len "$s")) in
    do:  ((![#ptrT] "s") <-[#children] "$r0");;;
    do:  ("toClear" <-[#children] "$r1");;;
    (for: (λ: <>, int_gt (let: "$a0" := (![#children] "toClear") in
    slice.len "$a0") #(W64 0)); (λ: <>, #()) := λ: <>,
      let: "$r0" := (let: "$s" := (![#children] "toClear") in
      slice.slice #ptrT "$s" (let: "$a0" := (![#children] "toClear") in
      let: "$a1" := (![#children] (globals.get #nilChildren)) in
      (slice.copy #ptrT) "$a0" "$a1") (slice.len "$s")) in
      do:  ("toClear" <-[#children] "$r0"));;;
    return: #()).

(* go: btree.go:254:16 *)
Definition node__mutableForⁱᵐᵖˡ : val :=
  λ: "n" "cow",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "cow" := (mem.alloc "cow") in
    (if: (![#ptrT] (struct.field_ref #node #"cow"%go (![#ptrT] "n"))) = (![#ptrT] "cow")
    then return: (![#ptrT] "n")
    else do:  #());;;
    let: "out" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := ((method_call #(ptrT.id copyOnWriteContext.id) #"newNode"%go (![#ptrT] "cow")) #()) in
    do:  ("out" <-[#ptrT] "$r0");;;
    (if: int_geq (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "out"))) in
    slice.cap "$a0") (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
    slice.len "$a0")
    then
      let: "$r0" := (let: "$s" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "out"))) in
      slice.slice #Item "$s" #(W64 0) (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
      slice.len "$a0")) in
      do:  ((struct.field_ref #node #"items"%go (![#ptrT] "out")) <-[#items] "$r0")
    else
      let: "$r0" := (slice.make3 #Item (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
      slice.len "$a0") (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
      slice.cap "$a0")) in
      do:  ((struct.field_ref #node #"items"%go (![#ptrT] "out")) <-[#items] "$r0"));;;
    do:  (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "out"))) in
    let: "$a1" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
    (slice.copy #Item) "$a0" "$a1");;;
    (if: int_geq (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "out"))) in
    slice.cap "$a0") (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    slice.len "$a0")
    then
      let: "$r0" := (let: "$s" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "out"))) in
      slice.slice #ptrT "$s" #(W64 0) (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.len "$a0")) in
      do:  ((struct.field_ref #node #"children"%go (![#ptrT] "out")) <-[#children] "$r0")
    else
      let: "$r0" := (slice.make3 #ptrT (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.len "$a0") (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.cap "$a0")) in
      do:  ((struct.field_ref #node #"children"%go (![#ptrT] "out")) <-[#children] "$r0"));;;
    do:  (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "out"))) in
    let: "$a1" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    (slice.copy #ptrT) "$a0" "$a1");;;
    return: (![#ptrT] "out")).

(* go: btree.go:275:16 *)
Definition node__mutableChildⁱᵐᵖˡ : val :=
  λ: "n" "i",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "i" := (mem.alloc "i") in
    let: "c" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (![#ptrT] (struct.field_ref #node #"cow"%go (![#ptrT] "n"))) in
    (method_call #(ptrT.id node.id) #"mutableFor"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) (![#intT] "i")))) "$a0") in
    do:  ("c" <-[#ptrT] "$r0");;;
    let: "$r0" := (![#ptrT] "c") in
    do:  ((slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) (![#intT] "i")) <-[#ptrT] "$r0");;;
    return: (![#ptrT] "c")).

(* split splits the given node at the given index.  The current node shrinks,
   and this function returns the item that existed at that index and a new node
   containing all items/children after it.

   go: btree.go:284:16 *)
Definition node__splitⁱᵐᵖˡ : val :=
  λ: "n" "i",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "i" := (mem.alloc "i") in
    let: "item" := (mem.alloc (type.zero_val #Item)) in
    let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
    do:  ("item" <-[#Item] "$r0");;;
    let: "next" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := ((method_call #(ptrT.id copyOnWriteContext.id) #"newNode"%go (![#ptrT] (struct.field_ref #node #"cow"%go (![#ptrT] "n")))) #()) in
    do:  ("next" <-[#ptrT] "$r0");;;
    let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "next"))) in
    let: "$a1" := (let: "$s" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
    slice.slice #Item "$s" ((![#intT] "i") + #(W64 1)) (slice.len "$s")) in
    (slice.append #Item) "$a0" "$a1") in
    do:  ((struct.field_ref #node #"items"%go (![#ptrT] "next")) <-[#items] "$r0");;;
    do:  (let: "$a0" := (![#intT] "i") in
    (method_call #(ptrT.id items.id) #"truncate"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0");;;
    (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    slice.len "$a0") #(W64 0)
    then
      let: "$r0" := (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "next"))) in
      let: "$a1" := (let: "$s" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.slice #ptrT "$s" ((![#intT] "i") + #(W64 1)) (slice.len "$s")) in
      (slice.append #ptrT) "$a0" "$a1") in
      do:  ((struct.field_ref #node #"children"%go (![#ptrT] "next")) <-[#children] "$r0");;;
      do:  (let: "$a0" := ((![#intT] "i") + #(W64 1)) in
      (method_call #(ptrT.id children.id) #"truncate"%go (struct.field_ref #node #"children"%go (![#ptrT] "n"))) "$a0")
    else do:  #());;;
    return: (![#Item] "item", ![#ptrT] "next")).

(* maybeSplitChild checks if a child should be split, and if so splits it.
   Returns whether or not a split occurred.

   go: btree.go:298:16 *)
Definition node__maybeSplitChildⁱᵐᵖˡ : val :=
  λ: "n" "i" "maxItems",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "maxItems" := (mem.alloc "maxItems") in
    let: "i" := (mem.alloc "i") in
    (if: int_lt (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) (![#intT] "i"))))) in
    slice.len "$a0") (![#intT] "maxItems")
    then return: (#false)
    else do:  #());;;
    let: "first" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (![#intT] "i") in
    (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
    do:  ("first" <-[#ptrT] "$r0");;;
    let: "second" := (mem.alloc (type.zero_val #ptrT)) in
    let: "item" := (mem.alloc (type.zero_val #Item)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := ((![#intT] "maxItems") `quots` #(W64 2)) in
    (method_call #(ptrT.id node.id) #"split"%go (![#ptrT] "first")) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("item" <-[#Item] "$r0");;;
    do:  ("second" <-[#ptrT] "$r1");;;
    do:  (let: "$a0" := (![#intT] "i") in
    let: "$a1" := (![#Item] "item") in
    (method_call #(ptrT.id items.id) #"insertAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0" "$a1");;;
    do:  (let: "$a0" := ((![#intT] "i") + #(W64 1)) in
    let: "$a1" := (![#ptrT] "second") in
    (method_call #(ptrT.id children.id) #"insertAt"%go (struct.field_ref #node #"children"%go (![#ptrT] "n"))) "$a0" "$a1");;;
    return: (#true)).

(* insert inserts an item into the subtree rooted at this node, making sure
   no nodes in the subtree exceed maxItems items.  Should an equivalent item be
   be found/replaced by insert, it will be returned.

   go: btree.go:312:16 *)
Definition node__insertⁱᵐᵖˡ : val :=
  λ: "n" "item" "maxItems",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "maxItems" := (mem.alloc "maxItems") in
    let: "item" := (mem.alloc "item") in
    let: "found" := (mem.alloc (type.zero_val #boolT)) in
    let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#Item] "item") in
    (method_call #items.id #"find"%go (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n")))) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("i" <-[#intT] "$r0");;;
    do:  ("found" <-[#boolT] "$r1");;;
    (if: ![#boolT] "found"
    then
      let: "out" := (mem.alloc (type.zero_val #Item)) in
      let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
      do:  ("out" <-[#Item] "$r0");;;
      let: "$r0" := (![#Item] "item") in
      do:  ((slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")) <-[#Item] "$r0");;;
      return: (![#Item] "out")
    else do:  #());;;
    (if: (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    slice.len "$a0") = #(W64 0)
    then
      do:  (let: "$a0" := (![#intT] "i") in
      let: "$a1" := (![#Item] "item") in
      (method_call #(ptrT.id items.id) #"insertAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0" "$a1");;;
      return: (#interface.nil)
    else do:  #());;;
    (if: let: "$a0" := (![#intT] "i") in
    let: "$a1" := (![#intT] "maxItems") in
    (method_call #(ptrT.id node.id) #"maybeSplitChild"%go (![#ptrT] "n")) "$a0" "$a1"
    then
      let: "inTree" := (mem.alloc (type.zero_val #Item)) in
      let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
      do:  ("inTree" <-[#Item] "$r0");;;
      let: "$sw" := #true in
      (if: "$sw" = (let: "$a0" := (![#Item] "inTree") in
      (interface.get #"Less"%go (![#Item] "item")) "$a0")
      then do:  #()
      else
        (if: "$sw" = (let: "$a0" := (![#Item] "item") in
        (interface.get #"Less"%go (![#Item] "inTree")) "$a0")
        then do:  ("i" <-[#intT] ((![#intT] "i") + #(W64 1)))
        else
          let: "out" := (mem.alloc (type.zero_val #Item)) in
          let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
          do:  ("out" <-[#Item] "$r0");;;
          let: "$r0" := (![#Item] "item") in
          do:  ((slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")) <-[#Item] "$r0");;;
          return: (![#Item] "out")))
    else do:  #());;;
    return: (let: "$a0" := (![#Item] "item") in
     let: "$a1" := (![#intT] "maxItems") in
     (method_call #(ptrT.id node.id) #"insert"%go (let: "$a0" := (![#intT] "i") in
     (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0")) "$a0" "$a1")).

(* get finds the given key in the subtree and returns it.

   go: btree.go:340:16 *)
Definition node__getⁱᵐᵖˡ : val :=
  λ: "n" "key",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "key" := (mem.alloc "key") in
    let: "found" := (mem.alloc (type.zero_val #boolT)) in
    let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: ("$ret0", "$ret1") := (let: "$a0" := (![#Item] "key") in
    (method_call #items.id #"find"%go (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n")))) "$a0") in
    let: "$r0" := "$ret0" in
    let: "$r1" := "$ret1" in
    do:  ("i" <-[#intT] "$r0");;;
    do:  ("found" <-[#boolT] "$r1");;;
    (if: ![#boolT] "found"
    then return: (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")))
    else
      (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.len "$a0") #(W64 0)
      then
        return: (let: "$a0" := (![#Item] "key") in
         (method_call #(ptrT.id node.id) #"get"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) (![#intT] "i")))) "$a0")
      else do:  #()));;;
    return: (#interface.nil)).

Definition min : go_string := "btree.min"%go.

(* min returns the first item in the subtree.

   go: btree.go:351:6 *)
Definition minⁱᵐᵖˡ : val :=
  λ: "n",
    exception_do (let: "n" := (mem.alloc "n") in
    (if: (![#ptrT] "n") = #null
    then return: (#interface.nil)
    else do:  #());;;
    (for: (λ: <>, int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    slice.len "$a0") #(W64 0)); (λ: <>, #()) := λ: <>,
      let: "$r0" := (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) #(W64 0))) in
      do:  ("n" <-[#ptrT] "$r0"));;;
    (if: (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
    slice.len "$a0") = #(W64 0)
    then return: (#interface.nil)
    else do:  #());;;
    return: (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) #(W64 0)))).

Definition max : go_string := "btree.max"%go.

(* max returns the last item in the subtree.

   go: btree.go:365:6 *)
Definition maxⁱᵐᵖˡ : val :=
  λ: "n",
    exception_do (let: "n" := (mem.alloc "n") in
    (if: (![#ptrT] "n") = #null
    then return: (#interface.nil)
    else do:  #());;;
    (for: (λ: <>, int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    slice.len "$a0") #(W64 0)); (λ: <>, #()) := λ: <>,
      let: "$r0" := (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) ((let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.len "$a0") - #(W64 1)))) in
      do:  ("n" <-[#ptrT] "$r0"));;;
    (if: (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
    slice.len "$a0") = #(W64 0)
    then return: (#interface.nil)
    else do:  #());;;
    return: (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) ((let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
     slice.len "$a0") - #(W64 1))))).

Definition toRemove : go_type := intT.
#[global] Typeclasses Opaque toRemove.
#[global] Opaque toRemove.

(* removes the given item *)
Definition removeItem : val := #(W64 0).

(* removes smallest item in the subtree *)
Definition removeMin : val := #(W64 1).

(* removes largest item in the subtree *)
Definition removeMax : val := #(W64 2).

(* remove removes an item from the subtree rooted at this node.

   go: btree.go:388:16 *)
Definition node__removeⁱᵐᵖˡ : val :=
  λ: "n" "item" "minItems" "typ",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "typ" := (mem.alloc "typ") in
    let: "minItems" := (mem.alloc "minItems") in
    let: "item" := (mem.alloc "item") in
    let: "i" := (mem.alloc (type.zero_val #intT)) in
    let: "found" := (mem.alloc (type.zero_val #boolT)) in
    let: "$sw" := (![#toRemove] "typ") in
    (if: "$sw" = removeMax
    then
      (if: (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.len "$a0") = #(W64 0)
      then return: ((method_call #(ptrT.id items.id) #"pop"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) #())
      else do:  #());;;
      let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
      slice.len "$a0") in
      do:  ("i" <-[#intT] "$r0")
    else
      (if: "$sw" = removeMin
      then
        (if: (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
        slice.len "$a0") = #(W64 0)
        then
          return: (let: "$a0" := #(W64 0) in
           (method_call #(ptrT.id items.id) #"removeAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0")
        else do:  #());;;
        let: "$r0" := #(W64 0) in
        do:  ("i" <-[#intT] "$r0")
      else
        (if: "$sw" = removeItem
        then
          let: ("$ret0", "$ret1") := (let: "$a0" := (![#Item] "item") in
          (method_call #items.id #"find"%go (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n")))) "$a0") in
          let: "$r0" := "$ret0" in
          let: "$r1" := "$ret1" in
          do:  ("i" <-[#intT] "$r0");;;
          do:  ("found" <-[#boolT] "$r1");;;
          (if: (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
          slice.len "$a0") = #(W64 0)
          then
            (if: ![#boolT] "found"
            then
              return: (let: "$a0" := (![#intT] "i") in
               (method_call #(ptrT.id items.id) #"removeAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0")
            else do:  #());;;
            return: (#interface.nil)
          else do:  #())
        else
          do:  (let: "$a0" := (interface.make #stringT.id #"invalid type"%go) in
          Panic "$a0"))));;;
    (if: int_leq (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) (![#intT] "i"))))) in
    slice.len "$a0") (![#intT] "minItems")
    then
      return: (let: "$a0" := (![#intT] "i") in
       let: "$a1" := (![#Item] "item") in
       let: "$a2" := (![#intT] "minItems") in
       let: "$a3" := (![#toRemove] "typ") in
       (method_call #(ptrT.id node.id) #"growChildAndRemove"%go (![#ptrT] "n")) "$a0" "$a1" "$a2" "$a3")
    else do:  #());;;
    let: "child" := (mem.alloc (type.zero_val #ptrT)) in
    let: "$r0" := (let: "$a0" := (![#intT] "i") in
    (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
    do:  ("child" <-[#ptrT] "$r0");;;
    (if: ![#boolT] "found"
    then
      let: "out" := (mem.alloc (type.zero_val #Item)) in
      let: "$r0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
      do:  ("out" <-[#Item] "$r0");;;
      let: "$r0" := (let: "$a0" := #interface.nil in
      let: "$a1" := (![#intT] "minItems") in
      let: "$a2" := removeMax in
      (method_call #(ptrT.id node.id) #"remove"%go (![#ptrT] "child")) "$a0" "$a1" "$a2") in
      do:  ((slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")) <-[#Item] "$r0");;;
      return: (![#Item] "out")
    else do:  #());;;
    return: (let: "$a0" := (![#Item] "item") in
     let: "$a1" := (![#intT] "minItems") in
     let: "$a2" := (![#toRemove] "typ") in
     (method_call #(ptrT.id node.id) #"remove"%go (![#ptrT] "child")) "$a0" "$a1" "$a2")).

(* growChildAndRemove grows child 'i' to make sure it's possible to remove an
   item from it while keeping it at minItems, then calls remove to actually
   remove it.

   Most documentation says we have to do two sets of special casing:
     1) item is in this node
     2) item is in child
   In both cases, we need to handle the two subcases:
     A) node has enough values that it can spare one
     B) node doesn't have enough values
   For the latter, we have to check:
     a) left sibling has node to spare
     b) right sibling has node to spare
     c) we must merge
   To simplify our code here, we handle cases #1 and #2 the same:
   If a node doesn't have enough items, we make sure it does (using a,b,c).
   We then simply redo our remove call, and the second time (regardless of
   whether we're in case 1 or 2), we'll have enough items and can guarantee
   that we hit case A.

   go: btree.go:455:16 *)
Definition node__growChildAndRemoveⁱᵐᵖˡ : val :=
  λ: "n" "i" "item" "minItems" "typ",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "typ" := (mem.alloc "typ") in
    let: "minItems" := (mem.alloc "minItems") in
    let: "item" := (mem.alloc "item") in
    let: "i" := (mem.alloc "i") in
    (if: (int_gt (![#intT] "i") #(W64 0)) && (int_gt (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) ((![#intT] "i") - #(W64 1)))))) in
    slice.len "$a0") (![#intT] "minItems"))
    then
      let: "child" := (mem.alloc (type.zero_val #ptrT)) in
      let: "$r0" := (let: "$a0" := (![#intT] "i") in
      (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
      do:  ("child" <-[#ptrT] "$r0");;;
      let: "stealFrom" := (mem.alloc (type.zero_val #ptrT)) in
      let: "$r0" := (let: "$a0" := ((![#intT] "i") - #(W64 1)) in
      (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
      do:  ("stealFrom" <-[#ptrT] "$r0");;;
      let: "stolenItem" := (mem.alloc (type.zero_val #Item)) in
      let: "$r0" := ((method_call #(ptrT.id items.id) #"pop"%go (struct.field_ref #node #"items"%go (![#ptrT] "stealFrom"))) #()) in
      do:  ("stolenItem" <-[#Item] "$r0");;;
      do:  (let: "$a0" := #(W64 0) in
      let: "$a1" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) ((![#intT] "i") - #(W64 1)))) in
      (method_call #(ptrT.id items.id) #"insertAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "child"))) "$a0" "$a1");;;
      let: "$r0" := (![#Item] "stolenItem") in
      do:  ((slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) ((![#intT] "i") - #(W64 1))) <-[#Item] "$r0");;;
      (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "stealFrom"))) in
      slice.len "$a0") #(W64 0)
      then
        do:  (let: "$a0" := #(W64 0) in
        let: "$a1" := ((method_call #(ptrT.id children.id) #"pop"%go (struct.field_ref #node #"children"%go (![#ptrT] "stealFrom"))) #()) in
        (method_call #(ptrT.id children.id) #"insertAt"%go (struct.field_ref #node #"children"%go (![#ptrT] "child"))) "$a0" "$a1")
      else do:  #())
    else
      (if: (int_lt (![#intT] "i") (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
      slice.len "$a0")) && (int_gt (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) ((![#intT] "i") + #(W64 1)))))) in
      slice.len "$a0") (![#intT] "minItems"))
      then
        let: "child" := (mem.alloc (type.zero_val #ptrT)) in
        let: "$r0" := (let: "$a0" := (![#intT] "i") in
        (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
        do:  ("child" <-[#ptrT] "$r0");;;
        let: "stealFrom" := (mem.alloc (type.zero_val #ptrT)) in
        let: "$r0" := (let: "$a0" := ((![#intT] "i") + #(W64 1)) in
        (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
        do:  ("stealFrom" <-[#ptrT] "$r0");;;
        let: "stolenItem" := (mem.alloc (type.zero_val #Item)) in
        let: "$r0" := (let: "$a0" := #(W64 0) in
        (method_call #(ptrT.id items.id) #"removeAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "stealFrom"))) "$a0") in
        do:  ("stolenItem" <-[#Item] "$r0");;;
        let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "child"))) in
        let: "$a1" := ((let: "$sl0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
        slice.literal #Item ["$sl0"])) in
        (slice.append #Item) "$a0" "$a1") in
        do:  ((struct.field_ref #node #"items"%go (![#ptrT] "child")) <-[#items] "$r0");;;
        let: "$r0" := (![#Item] "stolenItem") in
        do:  ((slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")) <-[#Item] "$r0");;;
        (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "stealFrom"))) in
        slice.len "$a0") #(W64 0)
        then
          let: "$r0" := (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "child"))) in
          let: "$a1" := ((let: "$sl0" := (let: "$a0" := #(W64 0) in
          (method_call #(ptrT.id children.id) #"removeAt"%go (struct.field_ref #node #"children"%go (![#ptrT] "stealFrom"))) "$a0") in
          slice.literal #ptrT ["$sl0"])) in
          (slice.append #ptrT) "$a0" "$a1") in
          do:  ((struct.field_ref #node #"children"%go (![#ptrT] "child")) <-[#children] "$r0")
        else do:  #())
      else
        (if: int_geq (![#intT] "i") (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
        slice.len "$a0")
        then do:  ("i" <-[#intT] ((![#intT] "i") - #(W64 1)))
        else do:  #());;;
        let: "child" := (mem.alloc (type.zero_val #ptrT)) in
        let: "$r0" := (let: "$a0" := (![#intT] "i") in
        (method_call #(ptrT.id node.id) #"mutableChild"%go (![#ptrT] "n")) "$a0") in
        do:  ("child" <-[#ptrT] "$r0");;;
        let: "mergeItem" := (mem.alloc (type.zero_val #Item)) in
        let: "$r0" := (let: "$a0" := (![#intT] "i") in
        (method_call #(ptrT.id items.id) #"removeAt"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0") in
        do:  ("mergeItem" <-[#Item] "$r0");;;
        let: "mergeChild" := (mem.alloc (type.zero_val #ptrT)) in
        let: "$r0" := (let: "$a0" := (![#ptrT] (struct.field_ref #node #"cow"%go (![#ptrT] "n"))) in
        (method_call #(ptrT.id node.id) #"mutableFor"%go (let: "$a0" := ((![#intT] "i") + #(W64 1)) in
        (method_call #(ptrT.id children.id) #"removeAt"%go (struct.field_ref #node #"children"%go (![#ptrT] "n"))) "$a0")) "$a0") in
        do:  ("mergeChild" <-[#ptrT] "$r0");;;
        let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "child"))) in
        let: "$a1" := ((let: "$sl0" := (![#Item] "mergeItem") in
        slice.literal #Item ["$sl0"])) in
        (slice.append #Item) "$a0" "$a1") in
        do:  ((struct.field_ref #node #"items"%go (![#ptrT] "child")) <-[#items] "$r0");;;
        let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "child"))) in
        let: "$a1" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "mergeChild"))) in
        (slice.append #Item) "$a0" "$a1") in
        do:  ((struct.field_ref #node #"items"%go (![#ptrT] "child")) <-[#items] "$r0");;;
        let: "$r0" := (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "child"))) in
        let: "$a1" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "mergeChild"))) in
        (slice.append #ptrT) "$a0" "$a1") in
        do:  ((struct.field_ref #node #"children"%go (![#ptrT] "child")) <-[#children] "$r0");;;
        do:  (let: "$a0" := (![#ptrT] "mergeChild") in
        (method_call #(ptrT.id copyOnWriteContext.id) #"freeNode"%go (![#ptrT] (struct.field_ref #node #"cow"%go (![#ptrT] "n")))) "$a0")));;;
    return: (let: "$a0" := (![#Item] "item") in
     let: "$a1" := (![#intT] "minItems") in
     let: "$a2" := (![#toRemove] "typ") in
     (method_call #(ptrT.id node.id) #"remove"%go (![#ptrT] "n")) "$a0" "$a1" "$a2")).

Definition direction : go_type := intT.
#[global] Typeclasses Opaque direction.
#[global] Opaque direction.

Definition descend : val := #(W64 (-1)).

Definition ascend : val := #(W64 1).

(* iterate provides a simple method for iterating over elements in the tree.

   When ascending, the 'start' should be less than 'stop' and when descending,
   the 'start' should be greater than 'stop'. Setting 'includeStart' to true
   will force the iterator to include the first item when it equals 'start',
   thus creating a "greaterOrEqual" or "lessThanEqual" rather than just a
   "greaterThan" or "lessThan" queries.

   go: btree.go:506:16 *)
Definition node__iterateⁱᵐᵖˡ : val :=
  λ: "n" "dir" "start" "stop" "includeStart" "hit" "iter",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "iter" := (mem.alloc "iter") in
    let: "hit" := (mem.alloc "hit") in
    let: "includeStart" := (mem.alloc "includeStart") in
    let: "stop" := (mem.alloc "stop") in
    let: "start" := (mem.alloc "start") in
    let: "dir" := (mem.alloc "dir") in
    let: "found" := (mem.alloc (type.zero_val #boolT)) in
    let: "ok" := (mem.alloc (type.zero_val #boolT)) in
    let: "index" := (mem.alloc (type.zero_val #intT)) in
    let: "$sw" := (![#direction] "dir") in
    (if: "$sw" = ascend
    then
      (if: (~ (interface.eq (![#Item] "start") #interface.nil))
      then
        let: ("$ret0", "$ret1") := (let: "$a0" := (![#Item] "start") in
        (method_call #items.id #"find"%go (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n")))) "$a0") in
        let: "$r0" := "$ret0" in
        let: "$r1" := "$ret1" in
        do:  ("index" <-[#intT] "$r0");;;
        do:  "$r1"
      else do:  #());;;
      (let: "i" := (mem.alloc (type.zero_val #intT)) in
      let: "$r0" := (![#intT] "index") in
      do:  ("i" <-[#intT] "$r0");;;
      (for: (λ: <>, int_lt (![#intT] "i") (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
      slice.len "$a0")); (λ: <>, do:  ("i" <-[#intT] ((![#intT] "i") + #(W64 1)))) := λ: <>,
        (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
        slice.len "$a0") #(W64 0)
        then
          (let: ("$ret0", "$ret1") := (let: "$a0" := (![#direction] "dir") in
          let: "$a1" := (![#Item] "start") in
          let: "$a2" := (![#Item] "stop") in
          let: "$a3" := (![#boolT] "includeStart") in
          let: "$a4" := (![#boolT] "hit") in
          let: "$a5" := (![#ItemIterator] "iter") in
          (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) (![#intT] "i")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5") in
          let: "$r0" := "$ret0" in
          let: "$r1" := "$ret1" in
          do:  ("hit" <-[#boolT] "$r0");;;
          do:  ("ok" <-[#boolT] "$r1");;;
          (if: (~ (![#boolT] "ok"))
          then return: (![#boolT] "hit", #false)
          else do:  #()))
        else do:  #());;;
        (if: (((~ (![#boolT] "includeStart")) && (~ (![#boolT] "hit"))) && (~ (interface.eq (![#Item] "start") #interface.nil))) && (~ (let: "$a0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
        (interface.get #"Less"%go (![#Item] "start")) "$a0"))
        then
          let: "$r0" := #true in
          do:  ("hit" <-[#boolT] "$r0");;;
          continue: #()
        else do:  #());;;
        let: "$r0" := #true in
        do:  ("hit" <-[#boolT] "$r0");;;
        (if: (~ (interface.eq (![#Item] "stop") #interface.nil)) && (~ (let: "$a0" := (![#Item] "stop") in
        (interface.get #"Less"%go (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")))) "$a0"))
        then return: (![#boolT] "hit", #false)
        else do:  #());;;
        (if: (~ (let: "$a0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
        (![#ItemIterator] "iter") "$a0"))
        then return: (![#boolT] "hit", #false)
        else do:  #())));;;
      (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
      slice.len "$a0") #(W64 0)
      then
        (let: ("$ret0", "$ret1") := (let: "$a0" := (![#direction] "dir") in
        let: "$a1" := (![#Item] "start") in
        let: "$a2" := (![#Item] "stop") in
        let: "$a3" := (![#boolT] "includeStart") in
        let: "$a4" := (![#boolT] "hit") in
        let: "$a5" := (![#ItemIterator] "iter") in
        (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) ((let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
        slice.len "$a0") - #(W64 1))))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5") in
        let: "$r0" := "$ret0" in
        let: "$r1" := "$ret1" in
        do:  ("hit" <-[#boolT] "$r0");;;
        do:  ("ok" <-[#boolT] "$r1");;;
        (if: (~ (![#boolT] "ok"))
        then return: (![#boolT] "hit", #false)
        else do:  #()))
      else do:  #())
    else
      (if: "$sw" = descend
      then
        (if: (~ (interface.eq (![#Item] "start") #interface.nil))
        then
          let: ("$ret0", "$ret1") := (let: "$a0" := (![#Item] "start") in
          (method_call #items.id #"find"%go (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n")))) "$a0") in
          let: "$r0" := "$ret0" in
          let: "$r1" := "$ret1" in
          do:  ("index" <-[#intT] "$r0");;;
          do:  ("found" <-[#boolT] "$r1");;;
          (if: (~ (![#boolT] "found"))
          then
            let: "$r0" := ((![#intT] "index") - #(W64 1)) in
            do:  ("index" <-[#intT] "$r0")
          else do:  #())
        else
          let: "$r0" := ((let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) in
          slice.len "$a0") - #(W64 1)) in
          do:  ("index" <-[#intT] "$r0"));;;
        (let: "i" := (mem.alloc (type.zero_val #intT)) in
        let: "$r0" := (![#intT] "index") in
        do:  ("i" <-[#intT] "$r0");;;
        (for: (λ: <>, int_geq (![#intT] "i") #(W64 0)); (λ: <>, do:  ("i" <-[#intT] ((![#intT] "i") - #(W64 1)))) := λ: <>,
          (if: (~ (interface.eq (![#Item] "start") #interface.nil)) && (~ (let: "$a0" := (![#Item] "start") in
          (interface.get #"Less"%go (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i")))) "$a0"))
          then
            (if: ((~ (![#boolT] "includeStart")) || (![#boolT] "hit")) || (let: "$a0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
            (interface.get #"Less"%go (![#Item] "start")) "$a0")
            then continue: #()
            else do:  #())
          else do:  #());;;
          (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
          slice.len "$a0") #(W64 0)
          then
            (let: ("$ret0", "$ret1") := (let: "$a0" := (![#direction] "dir") in
            let: "$a1" := (![#Item] "start") in
            let: "$a2" := (![#Item] "stop") in
            let: "$a3" := (![#boolT] "includeStart") in
            let: "$a4" := (![#boolT] "hit") in
            let: "$a5" := (![#ItemIterator] "iter") in
            (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) ((![#intT] "i") + #(W64 1))))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5") in
            let: "$r0" := "$ret0" in
            let: "$r1" := "$ret1" in
            do:  ("hit" <-[#boolT] "$r0");;;
            do:  ("ok" <-[#boolT] "$r1");;;
            (if: (~ (![#boolT] "ok"))
            then return: (![#boolT] "hit", #false)
            else do:  #()))
          else do:  #());;;
          (if: (~ (interface.eq (![#Item] "stop") #interface.nil)) && (~ (let: "$a0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
          (interface.get #"Less"%go (![#Item] "stop")) "$a0"))
          then return: (![#boolT] "hit", #false)
          else do:  #());;;
          let: "$r0" := #true in
          do:  ("hit" <-[#boolT] "$r0");;;
          (if: (~ (let: "$a0" := (![#Item] (slice.elem_ref #Item (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n"))) (![#intT] "i"))) in
          (![#ItemIterator] "iter") "$a0"))
          then return: (![#boolT] "hit", #false)
          else do:  #())));;;
        (if: int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
        slice.len "$a0") #(W64 0)
        then
          (let: ("$ret0", "$ret1") := (let: "$a0" := (![#direction] "dir") in
          let: "$a1" := (![#Item] "start") in
          let: "$a2" := (![#Item] "stop") in
          let: "$a3" := (![#boolT] "includeStart") in
          let: "$a4" := (![#boolT] "hit") in
          let: "$a5" := (![#ItemIterator] "iter") in
          (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) #(W64 0)))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5") in
          let: "$r0" := "$ret0" in
          let: "$r1" := "$ret1" in
          do:  ("hit" <-[#boolT] "$r0");;;
          do:  ("ok" <-[#boolT] "$r1");;;
          (if: (~ (![#boolT] "ok"))
          then return: (![#boolT] "hit", #false)
          else do:  #()))
        else do:  #())
      else do:  #()));;;
    return: (![#boolT] "hit", #true)).

(* Used for testing/debugging purposes.

   go: btree.go:575:16 *)
Definition node__printⁱᵐᵖˡ : val :=
  λ: "n" "w" "level",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "level" := (mem.alloc "level") in
    let: "w" := (mem.alloc "w") in
    do:  (let: "$a0" := (![#io.Writer] "w") in
    let: "$a1" := #"%sNODE:%v
    "%go in
    let: "$a2" := ((let: "$sl0" := (interface.make #stringT.id (let: "$a0" := #"  "%go in
    let: "$a1" := (![#intT] "level") in
    (func_call #strings.Repeat) "$a0" "$a1")) in
    let: "$sl1" := (interface.make #items.id (![#items] (struct.field_ref #node #"items"%go (![#ptrT] "n")))) in
    slice.literal #interfaceT ["$sl0"; "$sl1"])) in
    (func_call #fmt.Fprintf) "$a0" "$a1" "$a2");;;
    let: "$range" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    (let: "c" := (mem.alloc (type.zero_val #ptrT)) in
    slice.for_range #ptrT "$range" (λ: "$key" "$value",
      do:  ("c" <-[#ptrT] "$value");;;
      do:  "$key";;;
      do:  (let: "$a0" := (![#io.Writer] "w") in
      let: "$a1" := ((![#intT] "level") + #(W64 1)) in
      (method_call #(ptrT.id node.id) #"print"%go (![#ptrT] "c")) "$a0" "$a1")));;;
    return: #()).

(* Clone clones the btree, lazily.  Clone should not be called concurrently,
   but the original tree (t) and the new tree (t2) can be used concurrently
   once the Clone call completes.

   The internal tree structure of b is marked read-only and shared between t and
   t2.  Writes to both t and t2 use copy-on-write logic, creating new nodes
   whenever one of b's original nodes would have been modified.  Read operations
   should have no performance degredation.  Write operations for both t and t2
   will initially experience minor slow-downs caused by additional allocs and
   copies due to the aforementioned copy-on-write logic, but should converge to
   the original performance characteristics of the original tree.

   go: btree.go:625:17 *)
Definition BTree__Cloneⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t2" := (mem.alloc (type.zero_val #ptrT)) in
    let: "t" := (mem.alloc "t") in
    let: "cow2" := (mem.alloc (type.zero_val #copyOnWriteContext)) in
    let: "cow1" := (mem.alloc (type.zero_val #copyOnWriteContext)) in
    let: "$r0" := (![#copyOnWriteContext] (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t")))) in
    let: "$r1" := (![#copyOnWriteContext] (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t")))) in
    do:  ("cow1" <-[#copyOnWriteContext] "$r0");;;
    do:  ("cow2" <-[#copyOnWriteContext] "$r1");;;
    let: "out" := (mem.alloc (type.zero_val #BTree)) in
    let: "$r0" := (![#BTree] (![#ptrT] "t")) in
    do:  ("out" <-[#BTree] "$r0");;;
    let: "$r0" := "cow1" in
    do:  ((struct.field_ref #BTree #"cow"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
    let: "$r0" := "cow2" in
    do:  ((struct.field_ref #BTree #"cow"%go "out") <-[#ptrT] "$r0");;;
    return: ("out")).

(* maxItems returns the max number of items to allow per node.

   go: btree.go:639:17 *)
Definition BTree__maxItemsⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: (((![#intT] (struct.field_ref #BTree #"degree"%go (![#ptrT] "t"))) * #(W64 2)) - #(W64 1))).

(* minItems returns the min number of items to allow per node (ignored for the
   root node).

   go: btree.go:645:17 *)
Definition BTree__minItemsⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: ((![#intT] (struct.field_ref #BTree #"degree"%go (![#ptrT] "t"))) - #(W64 1))).

(* go: btree.go:649:30 *)
Definition copyOnWriteContext__newNodeⁱᵐᵖˡ : val :=
  λ: "c" <>,
    exception_do (let: "n" := (mem.alloc (type.zero_val #ptrT)) in
    let: "c" := (mem.alloc "c") in
    let: "$r0" := ((method_call #(ptrT.id FreeList.id) #"newNode"%go (![#ptrT] (struct.field_ref #copyOnWriteContext #"freelist"%go (![#ptrT] "c")))) #()) in
    do:  ("n" <-[#ptrT] "$r0");;;
    let: "$r0" := (![#ptrT] "c") in
    do:  ((struct.field_ref #node #"cow"%go (![#ptrT] "n")) <-[#ptrT] "$r0");;;
    return: (![#ptrT] "n")).

Definition freeType : go_type := intT.
#[global] Typeclasses Opaque freeType.
#[global] Opaque freeType.

(* node was freed (available for GC, not stored in freelist) *)
Definition ftFreelistFull : val := #(W64 0).

(* node was stored in the freelist for later use *)
Definition ftStored : val := #(W64 1).

(* node was ignored by COW, since it's owned by another one *)
Definition ftNotOwned : val := #(W64 2).

(* freeNode frees a node within a given COW context, if it's owned by that
   context.  It returns what happened to the node (see freeType const
   documentation).

   go: btree.go:666:30 *)
Definition copyOnWriteContext__freeNodeⁱᵐᵖˡ : val :=
  λ: "c" "n",
    exception_do (let: "c" := (mem.alloc "c") in
    let: "n" := (mem.alloc "n") in
    (if: (![#ptrT] (struct.field_ref #node #"cow"%go (![#ptrT] "n"))) = (![#ptrT] "c")
    then
      do:  (let: "$a0" := #(W64 0) in
      (method_call #(ptrT.id items.id) #"truncate"%go (struct.field_ref #node #"items"%go (![#ptrT] "n"))) "$a0");;;
      do:  (let: "$a0" := #(W64 0) in
      (method_call #(ptrT.id children.id) #"truncate"%go (struct.field_ref #node #"children"%go (![#ptrT] "n"))) "$a0");;;
      let: "$r0" := #null in
      do:  ((struct.field_ref #node #"cow"%go (![#ptrT] "n")) <-[#ptrT] "$r0");;;
      (if: let: "$a0" := (![#ptrT] "n") in
      (method_call #(ptrT.id FreeList.id) #"freeNode"%go (![#ptrT] (struct.field_ref #copyOnWriteContext #"freelist"%go (![#ptrT] "c")))) "$a0"
      then return: (ftStored)
      else return: (ftFreelistFull))
    else return: (ftNotOwned))).

(* ReplaceOrInsert adds the given item to the tree.  If an item in the tree
   already equals the given one, it is removed from the tree and returned.
   Otherwise, nil is returned.

   nil cannot be added to the tree (will panic).

   go: btree.go:687:17 *)
Definition BTree__ReplaceOrInsertⁱᵐᵖˡ : val :=
  λ: "t" "item",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "item" := (mem.alloc "item") in
    (if: interface.eq (![#Item] "item") #interface.nil
    then
      do:  (let: "$a0" := (interface.make #stringT.id #"nil item being added to BTree"%go) in
      Panic "$a0")
    else do:  #());;;
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then
      let: "$r0" := ((method_call #(ptrT.id copyOnWriteContext.id) #"newNode"%go (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t")))) #()) in
      do:  ((struct.field_ref #BTree #"root"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
      let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
      let: "$a1" := ((let: "$sl0" := (![#Item] "item") in
      slice.literal #Item ["$sl0"])) in
      (slice.append #Item) "$a0" "$a1") in
      do:  ((struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) <-[#items] "$r0");;;
      do:  ((struct.field_ref #BTree #"length"%go (![#ptrT] "t")) <-[#intT] ((![#intT] (struct.field_ref #BTree #"length"%go (![#ptrT] "t"))) + #(W64 1)));;;
      return: (#interface.nil)
    else
      let: "$r0" := (let: "$a0" := (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t"))) in
      (method_call #(ptrT.id node.id) #"mutableFor"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0") in
      do:  ((struct.field_ref #BTree #"root"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
      (if: int_geq (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
      slice.len "$a0") ((method_call #(ptrT.id BTree.id) #"maxItems"%go (![#ptrT] "t")) #())
      then
        let: "second" := (mem.alloc (type.zero_val #ptrT)) in
        let: "item2" := (mem.alloc (type.zero_val #Item)) in
        let: ("$ret0", "$ret1") := (let: "$a0" := (((method_call #(ptrT.id BTree.id) #"maxItems"%go (![#ptrT] "t")) #()) `quots` #(W64 2)) in
        (method_call #(ptrT.id node.id) #"split"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0") in
        let: "$r0" := "$ret0" in
        let: "$r1" := "$ret1" in
        do:  ("item2" <-[#Item] "$r0");;;
        do:  ("second" <-[#ptrT] "$r1");;;
        let: "oldroot" := (mem.alloc (type.zero_val #ptrT)) in
        let: "$r0" := (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) in
        do:  ("oldroot" <-[#ptrT] "$r0");;;
        let: "$r0" := ((method_call #(ptrT.id copyOnWriteContext.id) #"newNode"%go (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t")))) #()) in
        do:  ((struct.field_ref #BTree #"root"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
        let: "$r0" := (let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
        let: "$a1" := ((let: "$sl0" := (![#Item] "item2") in
        slice.literal #Item ["$sl0"])) in
        (slice.append #Item) "$a0" "$a1") in
        do:  ((struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) <-[#items] "$r0");;;
        let: "$r0" := (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
        let: "$a1" := ((let: "$sl0" := (![#ptrT] "oldroot") in
        let: "$sl1" := (![#ptrT] "second") in
        slice.literal #ptrT ["$sl0"; "$sl1"])) in
        (slice.append #ptrT) "$a0" "$a1") in
        do:  ((struct.field_ref #node #"children"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) <-[#children] "$r0")
      else do:  #()));;;
    let: "out" := (mem.alloc (type.zero_val #Item)) in
    let: "$r0" := (let: "$a0" := (![#Item] "item") in
    let: "$a1" := ((method_call #(ptrT.id BTree.id) #"maxItems"%go (![#ptrT] "t")) #()) in
    (method_call #(ptrT.id node.id) #"insert"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1") in
    do:  ("out" <-[#Item] "$r0");;;
    (if: interface.eq (![#Item] "out") #interface.nil
    then do:  ((struct.field_ref #BTree #"length"%go (![#ptrT] "t")) <-[#intT] ((![#intT] (struct.field_ref #BTree #"length"%go (![#ptrT] "t"))) + #(W64 1)))
    else do:  #());;;
    return: (![#Item] "out")).

(* Delete removes an item equal to the passed in item from the tree, returning
   it.  If no such item exists, returns nil.

   go: btree.go:715:17 *)
Definition BTree__Deleteⁱᵐᵖˡ : val :=
  λ: "t" "item",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "item" := (mem.alloc "item") in
    return: (let: "$a0" := (![#Item] "item") in
     let: "$a1" := removeItem in
     (method_call #(ptrT.id BTree.id) #"deleteItem"%go (![#ptrT] "t")) "$a0" "$a1")).

(* DeleteMin removes the smallest item in the tree and returns it.
   If no such item exists, returns nil.

   go: btree.go:721:17 *)
Definition BTree__DeleteMinⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: (let: "$a0" := #interface.nil in
     let: "$a1" := removeMin in
     (method_call #(ptrT.id BTree.id) #"deleteItem"%go (![#ptrT] "t")) "$a0" "$a1")).

(* DeleteMax removes the largest item in the tree and returns it.
   If no such item exists, returns nil.

   go: btree.go:727:17 *)
Definition BTree__DeleteMaxⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: (let: "$a0" := #interface.nil in
     let: "$a1" := removeMax in
     (method_call #(ptrT.id BTree.id) #"deleteItem"%go (![#ptrT] "t")) "$a0" "$a1")).

(* go: btree.go:731:17 *)
Definition BTree__deleteItemⁱᵐᵖˡ : val :=
  λ: "t" "item" "typ",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "typ" := (mem.alloc "typ") in
    let: "item" := (mem.alloc "item") in
    (if: ((![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null) || ((let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
    slice.len "$a0") = #(W64 0))
    then return: (#interface.nil)
    else do:  #());;;
    let: "$r0" := (let: "$a0" := (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t"))) in
    (method_call #(ptrT.id node.id) #"mutableFor"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0") in
    do:  ((struct.field_ref #BTree #"root"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
    let: "out" := (mem.alloc (type.zero_val #Item)) in
    let: "$r0" := (let: "$a0" := (![#Item] "item") in
    let: "$a1" := ((method_call #(ptrT.id BTree.id) #"minItems"%go (![#ptrT] "t")) #()) in
    let: "$a2" := (![#toRemove] "typ") in
    (method_call #(ptrT.id node.id) #"remove"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2") in
    do:  ("out" <-[#Item] "$r0");;;
    (if: ((let: "$a0" := (![#items] (struct.field_ref #node #"items"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
    slice.len "$a0") = #(W64 0)) && (int_gt (let: "$a0" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) in
    slice.len "$a0") #(W64 0))
    then
      let: "oldroot" := (mem.alloc (type.zero_val #ptrT)) in
      let: "$r0" := (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) in
      do:  ("oldroot" <-[#ptrT] "$r0");;;
      let: "$r0" := (![#ptrT] (slice.elem_ref #ptrT (![#children] (struct.field_ref #node #"children"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))))) #(W64 0))) in
      do:  ((struct.field_ref #BTree #"root"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
      do:  (let: "$a0" := (![#ptrT] "oldroot") in
      (method_call #(ptrT.id copyOnWriteContext.id) #"freeNode"%go (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t")))) "$a0")
    else do:  #());;;
    (if: (~ (interface.eq (![#Item] "out") #interface.nil))
    then do:  ((struct.field_ref #BTree #"length"%go (![#ptrT] "t")) <-[#intT] ((![#intT] (struct.field_ref #BTree #"length"%go (![#ptrT] "t"))) - #(W64 1)))
    else do:  #());;;
    return: (![#Item] "out")).

(* AscendRange calls the iterator for every value in the tree within the range
   [greaterOrEqual, lessThan), until iterator returns false.

   go: btree.go:750:17 *)
Definition BTree__AscendRangeⁱᵐᵖˡ : val :=
  λ: "t" "greaterOrEqual" "lessThan" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    let: "lessThan" := (mem.alloc "lessThan") in
    let: "greaterOrEqual" := (mem.alloc "greaterOrEqual") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := ascend in
    let: "$a1" := (![#Item] "greaterOrEqual") in
    let: "$a2" := (![#Item] "lessThan") in
    let: "$a3" := #true in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* AscendLessThan calls the iterator for every value in the tree within the range
   [first, pivot), until iterator returns false.

   go: btree.go:759:17 *)
Definition BTree__AscendLessThanⁱᵐᵖˡ : val :=
  λ: "t" "pivot" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    let: "pivot" := (mem.alloc "pivot") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := ascend in
    let: "$a1" := #interface.nil in
    let: "$a2" := (![#Item] "pivot") in
    let: "$a3" := #false in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* AscendGreaterOrEqual calls the iterator for every value in the tree within
   the range [pivot, last], until iterator returns false.

   go: btree.go:768:17 *)
Definition BTree__AscendGreaterOrEqualⁱᵐᵖˡ : val :=
  λ: "t" "pivot" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    let: "pivot" := (mem.alloc "pivot") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := ascend in
    let: "$a1" := (![#Item] "pivot") in
    let: "$a2" := #interface.nil in
    let: "$a3" := #true in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* Ascend calls the iterator for every value in the tree within the range
   [first, last], until iterator returns false.

   go: btree.go:777:17 *)
Definition BTree__Ascendⁱᵐᵖˡ : val :=
  λ: "t" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := ascend in
    let: "$a1" := #interface.nil in
    let: "$a2" := #interface.nil in
    let: "$a3" := #false in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* DescendRange calls the iterator for every value in the tree within the range
   [lessOrEqual, greaterThan), until iterator returns false.

   go: btree.go:786:17 *)
Definition BTree__DescendRangeⁱᵐᵖˡ : val :=
  λ: "t" "lessOrEqual" "greaterThan" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    let: "greaterThan" := (mem.alloc "greaterThan") in
    let: "lessOrEqual" := (mem.alloc "lessOrEqual") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := descend in
    let: "$a1" := (![#Item] "lessOrEqual") in
    let: "$a2" := (![#Item] "greaterThan") in
    let: "$a3" := #true in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* DescendLessOrEqual calls the iterator for every value in the tree within the range
   [pivot, first], until iterator returns false.

   go: btree.go:795:17 *)
Definition BTree__DescendLessOrEqualⁱᵐᵖˡ : val :=
  λ: "t" "pivot" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    let: "pivot" := (mem.alloc "pivot") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := descend in
    let: "$a1" := (![#Item] "pivot") in
    let: "$a2" := #interface.nil in
    let: "$a3" := #true in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* DescendGreaterThan calls the iterator for every value in the tree within
   the range [last, pivot), until iterator returns false.

   go: btree.go:804:17 *)
Definition BTree__DescendGreaterThanⁱᵐᵖˡ : val :=
  λ: "t" "pivot" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    let: "pivot" := (mem.alloc "pivot") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := descend in
    let: "$a1" := #interface.nil in
    let: "$a2" := (![#Item] "pivot") in
    let: "$a3" := #false in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* Descend calls the iterator for every value in the tree within the range
   [last, first], until iterator returns false.

   go: btree.go:813:17 *)
Definition BTree__Descendⁱᵐᵖˡ : val :=
  λ: "t" "iterator",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "iterator" := (mem.alloc "iterator") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#())
    else do:  #());;;
    do:  (let: "$a0" := descend in
    let: "$a1" := #interface.nil in
    let: "$a2" := #interface.nil in
    let: "$a3" := #false in
    let: "$a4" := #false in
    let: "$a5" := (![#ItemIterator] "iterator") in
    (method_call #(ptrT.id node.id) #"iterate"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0" "$a1" "$a2" "$a3" "$a4" "$a5");;;
    return: #()).

(* Get looks for the key item in the tree, returning it.  It returns nil if
   unable to find that item.

   go: btree.go:822:17 *)
Definition BTree__Getⁱᵐᵖˡ : val :=
  λ: "t" "key",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "key" := (mem.alloc "key") in
    (if: (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) = #null
    then return: (#interface.nil)
    else do:  #());;;
    return: (let: "$a0" := (![#Item] "key") in
     (method_call #(ptrT.id node.id) #"get"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0")).

(* Min returns the smallest item in the tree, or nil if the tree is empty.

   go: btree.go:830:17 *)
Definition BTree__Minⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: (let: "$a0" := (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) in
     (func_call #min) "$a0")).

(* Max returns the largest item in the tree, or nil if the tree is empty.

   go: btree.go:835:17 *)
Definition BTree__Maxⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: (let: "$a0" := (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) in
     (func_call #max) "$a0")).

(* Has returns true if the given key is in the tree.

   go: btree.go:840:17 *)
Definition BTree__Hasⁱᵐᵖˡ : val :=
  λ: "t" "key",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "key" := (mem.alloc "key") in
    return: ((~ (interface.eq (let: "$a0" := (![#Item] "key") in
     (method_call #(ptrT.id BTree.id) #"Get"%go (![#ptrT] "t")) "$a0") #interface.nil)))).

(* Len returns the number of items currently in the tree.

   go: btree.go:845:17 *)
Definition BTree__Lenⁱᵐᵖˡ : val :=
  λ: "t" <>,
    exception_do (let: "t" := (mem.alloc "t") in
    return: (![#intT] (struct.field_ref #BTree #"length"%go (![#ptrT] "t")))).

(* Clear removes all items from the btree.  If addNodesToFreelist is true,
   t's nodes are added to its freelist as part of this call, until the freelist
   is full.  Otherwise, the root node is simply dereferenced and the subtree
   left to Go's normal GC processes.

   This can be much faster
   than calling Delete on all elements, because that requires finding/removing
   each element in the tree and updating the tree accordingly.  It also is
   somewhat faster than creating a new tree to replace the old one, because
   nodes from the old tree are reclaimed into the freelist for use by the new
   one, instead of being lost to the garbage collector.

   This call takes:
     O(1): when addNodesToFreelist is false, this is a single operation.
     O(1): when the freelist is already full, it breaks out immediately
     O(freelist size):  when the freelist is empty and the nodes are all owned
         by this tree, nodes are added to the freelist until full.
     O(tree size):  when all nodes are owned by another tree, all nodes are
         iterated over looking for nodes to add to the freelist, and due to
         ownership, none are.

   go: btree.go:869:17 *)
Definition BTree__Clearⁱᵐᵖˡ : val :=
  λ: "t" "addNodesToFreelist",
    exception_do (let: "t" := (mem.alloc "t") in
    let: "addNodesToFreelist" := (mem.alloc "addNodesToFreelist") in
    (if: ((![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t"))) ≠ #null) && (![#boolT] "addNodesToFreelist")
    then
      do:  (let: "$a0" := (![#ptrT] (struct.field_ref #BTree #"cow"%go (![#ptrT] "t"))) in
      (method_call #(ptrT.id node.id) #"reset"%go (![#ptrT] (struct.field_ref #BTree #"root"%go (![#ptrT] "t")))) "$a0")
    else do:  #());;;
    let: "$r0" := #null in
    let: "$r1" := #(W64 0) in
    do:  ((struct.field_ref #BTree #"root"%go (![#ptrT] "t")) <-[#ptrT] "$r0");;;
    do:  ((struct.field_ref #BTree #"length"%go (![#ptrT] "t")) <-[#intT] "$r1");;;
    return: #()).

(* reset returns a subtree to the freelist.  It breaks out immediately if the
   freelist is full, since the only benefit of iterating is to fill that
   freelist up.  Returns true if parent reset call should continue.

   go: btree.go:879:16 *)
Definition node__resetⁱᵐᵖˡ : val :=
  λ: "n" "c",
    exception_do (let: "n" := (mem.alloc "n") in
    let: "c" := (mem.alloc "c") in
    let: "$range" := (![#children] (struct.field_ref #node #"children"%go (![#ptrT] "n"))) in
    (let: "child" := (mem.alloc (type.zero_val #ptrT)) in
    slice.for_range #ptrT "$range" (λ: "$key" "$value",
      do:  ("child" <-[#ptrT] "$value");;;
      do:  "$key";;;
      (if: (~ (let: "$a0" := (![#ptrT] "c") in
      (method_call #(ptrT.id node.id) #"reset"%go (![#ptrT] "child")) "$a0"))
      then return: (#false)
      else do:  #())));;;
    return: ((let: "$a0" := (![#ptrT] "n") in
     (method_call #(ptrT.id copyOnWriteContext.id) #"freeNode"%go (![#ptrT] "c")) "$a0") ≠ ftFreelistFull)).

Definition Int : go_type := intT.
#[global] Typeclasses Opaque Int.
#[global] Opaque Int.

(* Less returns true if int(a) < int(b).

   go: btree.go:892:14 *)
Definition Int__Lessⁱᵐᵖˡ : val :=
  λ: "a" "b",
    exception_do (let: "a" := (mem.alloc "a") in
    let: "b" := (mem.alloc "b") in
    return: (int_lt (![#Int] "a") (interface.type_assert (![#Item] "b") #Int.id))).

Definition vars' : list (go_string * go_type) := [(nilItems, items); (nilChildren, children)].

Definition functions' : list (go_string * val) := [(NewFreeList, NewFreeListⁱᵐᵖˡ); (New, Newⁱᵐᵖˡ); (NewWithFreeList, NewWithFreeListⁱᵐᵖˡ); (min, minⁱᵐᵖˡ); (max, maxⁱᵐᵖˡ)].

Definition msets' : list (go_string * (list (go_string * val))) := [(FreeList.id, []); (ptrT.id FreeList.id, [("freeNode"%go, FreeList__freeNodeⁱᵐᵖˡ); ("newNode"%go, FreeList__newNodeⁱᵐᵖˡ)]); (ItemIterator.id, []); (ptrT.id ItemIterator.id, []); (items.id, [("find"%go, items__findⁱᵐᵖˡ)]); (ptrT.id items.id, [("find"%go, (λ: "$r",
                 method_call #items.id #"find"%go (![#items] "$r")
                 )%V); ("insertAt"%go, items__insertAtⁱᵐᵖˡ); ("pop"%go, items__popⁱᵐᵖˡ); ("removeAt"%go, items__removeAtⁱᵐᵖˡ); ("truncate"%go, items__truncateⁱᵐᵖˡ)]); (children.id, []); (ptrT.id children.id, [("insertAt"%go, children__insertAtⁱᵐᵖˡ); ("pop"%go, children__popⁱᵐᵖˡ); ("removeAt"%go, children__removeAtⁱᵐᵖˡ); ("truncate"%go, children__truncateⁱᵐᵖˡ)]); (node.id, []); (ptrT.id node.id, [("get"%go, node__getⁱᵐᵖˡ); ("growChildAndRemove"%go, node__growChildAndRemoveⁱᵐᵖˡ); ("insert"%go, node__insertⁱᵐᵖˡ); ("iterate"%go, node__iterateⁱᵐᵖˡ); ("maybeSplitChild"%go, node__maybeSplitChildⁱᵐᵖˡ); ("mutableChild"%go, node__mutableChildⁱᵐᵖˡ); ("mutableFor"%go, node__mutableForⁱᵐᵖˡ); ("print"%go, node__printⁱᵐᵖˡ); ("remove"%go, node__removeⁱᵐᵖˡ); ("reset"%go, node__resetⁱᵐᵖˡ); ("split"%go, node__splitⁱᵐᵖˡ)]); (toRemove.id, []); (ptrT.id toRemove.id, []); (direction.id, []); (ptrT.id direction.id, []); (BTree.id, []); (ptrT.id BTree.id, [("Ascend"%go, BTree__Ascendⁱᵐᵖˡ); ("AscendGreaterOrEqual"%go, BTree__AscendGreaterOrEqualⁱᵐᵖˡ); ("AscendLessThan"%go, BTree__AscendLessThanⁱᵐᵖˡ); ("AscendRange"%go, BTree__AscendRangeⁱᵐᵖˡ); ("Clear"%go, BTree__Clearⁱᵐᵖˡ); ("Clone"%go, BTree__Cloneⁱᵐᵖˡ); ("Delete"%go, BTree__Deleteⁱᵐᵖˡ); ("DeleteMax"%go, BTree__DeleteMaxⁱᵐᵖˡ); ("DeleteMin"%go, BTree__DeleteMinⁱᵐᵖˡ); ("Descend"%go, BTree__Descendⁱᵐᵖˡ); ("DescendGreaterThan"%go, BTree__DescendGreaterThanⁱᵐᵖˡ); ("DescendLessOrEqual"%go, BTree__DescendLessOrEqualⁱᵐᵖˡ); ("DescendRange"%go, BTree__DescendRangeⁱᵐᵖˡ); ("Get"%go, BTree__Getⁱᵐᵖˡ); ("Has"%go, BTree__Hasⁱᵐᵖˡ); ("Len"%go, BTree__Lenⁱᵐᵖˡ); ("Max"%go, BTree__Maxⁱᵐᵖˡ); ("Min"%go, BTree__Minⁱᵐᵖˡ); ("ReplaceOrInsert"%go, BTree__ReplaceOrInsertⁱᵐᵖˡ); ("deleteItem"%go, BTree__deleteItemⁱᵐᵖˡ); ("maxItems"%go, BTree__maxItemsⁱᵐᵖˡ); ("minItems"%go, BTree__minItemsⁱᵐᵖˡ)]); (copyOnWriteContext.id, []); (ptrT.id copyOnWriteContext.id, [("freeNode"%go, copyOnWriteContext__freeNodeⁱᵐᵖˡ); ("newNode"%go, copyOnWriteContext__newNodeⁱᵐᵖˡ)]); (freeType.id, []); (ptrT.id freeType.id, []); (Int.id, [("Less"%go, Int__Lessⁱᵐᵖˡ)]); (ptrT.id Int.id, [("Less"%go, (λ: "$r",
                 method_call #Int.id #"Less"%go (![#Int] "$r")
                 )%V)])].

#[global] Instance info' : PkgInfo btree.btree :=
  {|
    pkg_vars := vars';
    pkg_functions := functions';
    pkg_msets := msets';
    pkg_imported_pkgs := [code.fmt.fmt; code.io.io; code.sort.sort; code.strings.strings; code.sync.sync];
  |}.

Definition initialize' : val :=
  λ: <>,
    package.init #btree.btree (λ: <>,
      exception_do (do:  (sync.initialize' #());;;
      do:  (strings.initialize' #());;;
      do:  (sort.initialize' #());;;
      do:  (io.initialize' #());;;
      do:  (fmt.initialize' #());;;
      do:  (package.alloc btree.btree #());;;
      let: "$r0" := (slice.make2 #Item #(W64 16)) in
      do:  ((globals.get #nilItems) <-[#items] "$r0");;;
      let: "$r0" := (slice.make2 #ptrT #(W64 16)) in
      do:  ((globals.get #nilChildren) <-[#children] "$r0"))
      ).

End code.
End btree.
